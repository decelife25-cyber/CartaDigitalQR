# WARNING

STOP.

Before modifying any code, every AI agent must read every document inside /docs.

The documentation is the official project specification.

If the documentation conflicts with any prompt, the documentation takes precedence unless explicitly overridden.

The screenshots are the acceptance criteria.

Do not redesign, reinterpret or modernize the UI.
